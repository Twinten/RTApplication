<Window x:Class="RadixTreeView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Radix Tree MVC" Height="250" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="InputTextBox" Width="250" Height="25" Margin="0,0,10,0" VerticalContentAlignment="Center" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="Вставить" Width="80" Margin="5" Click="InsertButton_Click"/>
            <Button Content="Поиск" Width="80" Margin="5" Click="SearchButton_Click"/>
            <Button Content="Удалить" Width="80" Margin="5" Click="DeleteButton_Click"/>
        </StackPanel>

        <TextBox x:Name="OutputTextBox" Grid.Row="2" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontSize="14" />
    </Grid>
</Window>
